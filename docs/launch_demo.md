# Ghostlink Demo Flow

1. Clone the repository.
2. Start the model manager and gateway using the demo compose stack.
3. Use the model load endpoint to verify the backend.
4. Open the landing page and share the GitHub Pages URL with prospects.

```bash
docker compose -f docker-compose.demo.yml up --build
```

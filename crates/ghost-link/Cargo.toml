[package]
name = "ghost-link"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
ghostlink-core = { path = "../ghostlink-core" }

# Production error handling
anyhow = "1"

# Logging
tracing = "0.1"
tracing-subscriber = "0.3"

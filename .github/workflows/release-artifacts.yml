name: Release Artifacts

'on':
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-release-artifacts:
    name: Build Release Artifacts (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Validate release tag format
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: |
          set -euo pipefail
          [[ "${GITHUB_REF_NAME}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+(-[A-Za-z0-9._-]+)?$ ]]

      - name: Detect signing key availability
        id: gpg-check
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        env:
          GPG_PRIVATE_KEY: ${{ secrets.RELEASE_GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.RELEASE_GPG_PASSPHRASE }}
        run: |
          set -euo pipefail
          if [[ -n "${GPG_PRIVATE_KEY}" && -n "${GPG_PASSPHRASE}" ]]; then
            echo "has_key=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_key=false" >> "$GITHUB_OUTPUT"
            echo "::warning::RELEASE_GPG_PRIVATE_KEY or RELEASE_GPG_PASSPHRASE is not set; release bundle will be unsigned."
          fi

      - name: Import GPG key
        if: startsWith(github.ref, 'refs/tags/') && steps.gpg-check.outputs.has_key == 'true'
        shell: bash
        env:
          GPG_PRIVATE_KEY: ${{ secrets.RELEASE_GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.RELEASE_GPG_PASSPHRASE }}
        run: |
          set -euo pipefail
          printf '%s' "$GPG_PRIVATE_KEY" | gpg --batch --import
          echo "pinentry-mode loopback" >> ~/.gnupg/gpg.conf
          echo "allow-loopback-pinentry" >> ~/.gnupg/gpg-agent.conf
          gpgconf --kill gpg-agent

      - name: Build release bundle
        env:
          GPG_PASSPHRASE: ${{ secrets.RELEASE_GPG_PASSPHRASE }}
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]] && [[ "${{ steps.gpg-check.outputs.has_key }}" == "true" ]]; then
            bash scripts/release_bundle.sh ./artifacts/release signed "${{ matrix.os }}"
          else
            bash scripts/release_bundle.sh ./artifacts/release unsigned "${{ matrix.os }}"
          fi

      - name: Generate SBOM
        if: matrix.os == 'ubuntu-latest'
        uses: anchore/sbom-action@v0
        with:
          file: ./artifacts/release/ghost-link-${{ matrix.os }}
          format: spdx-json
          output-file: ./artifacts/release/sbom.spdx.json

      - name: Attest release artifact provenance
        if: matrix.os == 'ubuntu-latest'
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: ./artifacts/release/ghost-link-${{ matrix.os }}

      - name: Upload release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ghost-link-release-${{ matrix.os }}
          path: artifacts/release/
          retention-days: 30

      - name: Extract CHANGELOG section for this version
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME#v}"
          awk -v ver="$version" '
            BEGIN { found = 0 }
            /^## \[/ {
              if (found) exit
              if (index($0, "[" ver "]") > 0) { found = 1; print; next }
              next
            }
            found { print }
          ' CHANGELOG.md > ./artifacts/release/release-notes.md
          if [[ ! -s ./artifacts/release/release-notes.md ]]; then
            echo "No matching CHANGELOG.md section found for ${GITHUB_REF_NAME}; see CHANGELOG.md for details." \
              > ./artifacts/release/release-notes.md
          fi

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v3
        with:
          name: Ghostlink ${{ github.ref_name }}
          body_path: ./artifacts/release/release-notes.md
          files: |
            ./artifacts/release/ghost-link-${{ matrix.os }}*
            ./artifacts/release/SHA256SUMS-${{ matrix.os }}*
            ./artifacts/release/sbom.spdx.json
          fail_on_unmatched_files: false
